import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zoodoha.app',
  appName: 'zoodoha',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
